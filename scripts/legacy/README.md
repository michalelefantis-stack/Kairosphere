# Legacy one-off scripts

Data-munging scripts used to build the original catalogue. Kept for reference
because they document how the data was assembled, but they are not part of any
build and are excluded from typechecking.

Anything still needed lives in `pipeline/` now, which is tested and runnable.
