# Copy to .env and fill in. .env is gitignored — never commit a real key.
GEMINI_API_KEY=

# Optional
PORT=8787
# ALLOWED_ORIGINS=https://kairosphere.app,https://www.kairosphere.app
# RATE_LIMIT_MAX=20
