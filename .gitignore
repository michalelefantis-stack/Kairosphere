# Secrets — never commit these.
.env
.env.*
!.env.example
*.pem
*.key

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
server/node_modules

# Build output
dist
dist-ssr
*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Pipeline run artifacts (regenerate with `python -m pipeline.run`)
pipeline/out/

# Capacitor / Android build output
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
android/capacitor-cordova-android-plugins/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scratch
temp_extract/

# 9MB AI Studio prompt-history export. Kept on disk, kept out of history —
# git history is permanent and this is a record, not source. Remove this line
# if you want it versioned.
migrated_prompt_history/
